class Car():
    def __init__(self, name, orientation, x, y, length):
        self.name = name
        self.orientation = orientation
        self.x = x
        self.y = y
        self.length = length


    def is_move_valid(self):
        """
        Gets new direction to move in (x, y).
        checks whether car is within the playing field, checks if car is going to move correct orientation.
        And checks the path is not blocked by other cars.
        (maybe put all different functionalities of this function in other functions.)
        """
        pass
      

    def function(self):
        pass
        